import { Box, Heading, Text } from '@chakra-ui/react'

export default function TrackingForm() {
  return (
    <Box>
      <Heading mb={4}>Add / Edit Entry</Heading>
      <Text>Form to log a new batch or update an existing one.</Text>
    </Box>
  )
}
