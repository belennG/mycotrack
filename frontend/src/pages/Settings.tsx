import { Box, Heading, Text } from '@chakra-ui/react'

export default function Settings() {
  return (
    <Box>
      <Heading mb={4}>Settings</Heading>
      <Text>Application preferences and sensor configuration.</Text>
    </Box>
  )
}
